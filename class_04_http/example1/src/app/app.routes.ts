import { Routes } from '@angular/router';

// Empty route table - this mini example only cares about HttpClient, not routing, so there's
// nothing to map yet. provideRouter(routes) is still wired up in app.config.ts either way.
export const routes: Routes = [];
