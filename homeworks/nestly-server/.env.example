DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=nestly_dev
PORT=3000
CORS_ORIGIN=*

# Auth (Homework 6). Change the secrets for anything that isn't local coursework.
JWT_ACCESS_SECRET=nestly-dev-access-secret
JWT_REFRESH_SECRET=nestly-dev-refresh-secret
# Deliberately short so you can watch the refresh flow kick in.
JWT_ACCESS_EXPIRES_IN=60s
JWT_REFRESH_EXPIRES_IN=7d
