import { Routes } from '@angular/router';

// Empty for this demo - no routes are registered yet. This array is wired into
// the app via provideRouter(routes) in app.config.ts, which enables the Router
// even before any routes are added.
export const routes: Routes = [];
